# Org/project for sentry-cli during Gradle source-map upload (EAS / CI).
# Auth: set SENTRY_AUTH_TOKEN in EAS Secrets or locally — do not commit auth.token here.
defaults.org=keke-f7
defaults.project=react-native
defaults.url=https://sentry.io/
