// Alias route file to match the project's security guide naming.
// Actual implementation lives in `mapsRoutes.js` + `mapsController.js`.
import mapsRoutes from './mapsRoutes.js';

export default mapsRoutes;

